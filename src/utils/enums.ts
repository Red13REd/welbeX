export enum columnSelectType {
    name = 'name',
    count = 'count',
    distance = 'distance',
}

export enum conditionSelectType {
    'more' = ">",
    'less' = "<",
    'equals' = "=",
    'contain' = '%',
}